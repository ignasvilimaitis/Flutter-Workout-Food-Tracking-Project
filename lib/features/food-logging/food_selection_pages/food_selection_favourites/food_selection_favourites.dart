import 'package:flutter/material.dart';

class FoodSelectionFavourites extends StatelessWidget {
  const FoodSelectionFavourites({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Favourites");
  }
}